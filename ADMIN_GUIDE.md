# Trulicare Administrator Operations Guide

- **Audit Trails**: Inspect security logs stored in the `AuditLog` collection.
- **User Management**: Monitor patient, caregiver, and pharmacist accounts.
- **System Diagnostics**: Check `/api/auth/me` and `/api/data/medicines` status.
