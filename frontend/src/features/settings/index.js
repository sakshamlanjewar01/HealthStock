/**
 * Barrel export for the settings feature.
 * Import the modal from here anywhere in the app:
 *
 *   import { SettingsModal } from '@/features/settings';
 */
export { default as SettingsModal } from './components/SettingsModal';
